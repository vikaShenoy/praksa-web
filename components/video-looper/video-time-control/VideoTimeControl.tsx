import styled from "styled-components"

const Bar = styled.div`
  width: 100%;
  height: 0.75rem;
  background-color: ${(props) => props.theme.colors.disabled};
`

const VideoTimeControl = () => {

  return
}

export default VideoTimeControl